<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>See Pokedex Details</title>
</head>
<body style="background-color:lightblue;">
    <h1><%= allPokemon.name %> </h1>
    <h3> Damages:</h3>
    <ul>
       <% for(pokemonKey in allPokemon.damages) { %>
        <li> <%= pokemonKey %> : <%=  allPokemon.damages[pokemonKey] %></li>  

      <%  } %>
    </ul>

      <h3> Stats:</h3>
      <ul>
         <% for(pokemonKey in allPokemon.stats) { %>
          <li> <%= pokemonKey %> : <%=  allPokemon.stats[pokemonKey] %></li>  
  
        <%  } %>

    </ul>
    <h3> Type:</h3>
    <ul>
       <% for(pokemonKey in allPokemon.type) { %>
        <li> <%= pokemonKey %> : <%=  allPokemon.type[pokemonKey] %></li>  

      <%  } %>
      <img src = "<%= allPokemon.img %>"/>

  </ul>

  </ul>
    <a href = "/pokedex/"> Back to Pokedex Index</a>
</body>
</html>